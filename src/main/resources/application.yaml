server:
  port: 9191
spring:
  application:
    name: server
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
#    sql 8 以后不需要写这个驱动了，springboot会自动识别
    url: jdbc:mysql://127.0.0.1:3306/video?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: video_user
    password: 123456
#  redis:
#    host: 127.0.0.1
#        password: ???
    lettuce:
      pool:
        max-active: 10
        max-idle: 10
        min-idle: 1
        time-between-eviction-runs: 10s
  jackson:
    default-property-inclusion: non_null # JSON处理时忽略非空字段
#  data:
#    redis:
#      host: 10.1.28.202
#      port: 6379
mybatis-plus:
  type-aliases-package: com.example.server.entity # 别名扫描包
logging:
  level:
    com.example.server: debug

minio:
  endpoint: http://127.0.0.1:9091
  accessKey: root
  secretKey: 12345678
  bucketName: my-bucket-test1